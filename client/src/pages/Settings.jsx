import React from 'react';

export default function Settings() {
  return (
    <div className="text-sm text-text-secondary">
      Settings page scaffolded. Panel port, token regeneration, and update actions can be wired
      here on top of existing backend when needed.
    </div>
  );
}

