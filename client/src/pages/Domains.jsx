import React from 'react';

export default function Domains() {
  // Full domain management UI can be expanded here; backend /api/nginx is ready.
  return (
    <div className="text-text-secondary text-sm">
      Domains management UI is scaffolded in backend; frontend controls can be added here next.
    </div>
  );
}

